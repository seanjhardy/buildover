node_modules
dist
dist-electron
dist-server
release
.env
.env.local
*.log
.DS_Store
.vite
chats
.buildover
./claude

# Tailscale-issued TLS certs/keys (never commit private keys)
*.ts.net.crt
*.ts.net.key

# Local git worktrees created for isolated work
.claude/worktrees/